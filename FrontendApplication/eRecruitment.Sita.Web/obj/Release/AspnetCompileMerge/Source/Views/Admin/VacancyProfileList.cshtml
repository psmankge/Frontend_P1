
@{
    ViewBag.Title = "Blank";
}

<section class="content-header">
    <h1>
        Vacancy Profile List
        @*<small>it all starts here</small>*@
    </h1>
    <ol class="breadcrumb">
        <li><a href="#"><i class="fa fa-dashboard"></i> Home</a></li>
        <li><a href="#">Administration</a></li>
        <li class="active">Vacancy Profile</li>
    </ol>
</section>
<!-- Main content -->
<section class="content">
    <!-- Default box -->
    <div class="box">
        <div class="box-header with-border">
            @*<h3 class="box-title">Organisation</h3>*@
            <div class="pull-left">
                <a href="@Url.Action("AddVacancyProfile", "Admin")" class="btn btn-primary btn-sm">
                    <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
                Create Vacancy Profile
                </a>
            </div>
            <div class="box-tools pull-right">
                <button type="button" class="btn btn-box-tool" data-widget="collapse" data-toggle="tooltip"
                        title="Collapse">
                    <i class="fa fa-minus"></i>
                </button>
                <button type="button" class="btn btn-box-tool" data-widget="remove" disabled data-toggle="tooltip" title="Remove">
                    <i class="fa fa-times"></i>
                </button>
            </div>
        </div>
        <div class="box-body">
            <div class="box">
                @*<div class="box-header">
                        <h3 class="box-title">Vacancies List</h3>
                    </div>*@
                <!-- /.box-header -->
                <div class="box-body">
                    <table id="example10" class="table table-bordered table-striped">
                        <thead>
                            <tr>                            
                                <th>Vacancy Name</th>
                                <th>Organisation</th>
                                <th>Date Created</th>
                                <th>Action</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>

                            @if (ViewBag.VacancyProfile != null)
                            {
                                foreach (var item in ViewBag.VacancyProfile)
                                {
                                    <tr>                               
                                        <td>@item.VacancyName</td>                                      
                                        <td>@item.OrganisationName</td>  
                                        <td>@item.CreatedDateView</td>                              
                                        <td>
                                   
                                            <a href="@Url.Action("EditVacancyProfile", "Admin", new { id = @item.VacancyProfileID})"><span class="fa fa-pencil" style="color:blue"></span> Edit</a>
                                            @*<div class="btn-group">
                                                <button type="button" class="btn btn-success">Action</button>
                                                <button type="button" class="btn btn-success dropdown-toggle" data-toggle="dropdown">
                                                    <span class="caret"></span>
                                                    <span class="sr-only">Toggle Dropdown</span>
                                                </button>
                                                <ul class="dropdown-menu" role="menu">
                                                    <li><a href="@Url.Action("EditVacancyProfile", "Admin", new { id = @item.VacancyProfileID})"><span class="fa fa-pencil" style="color:blue"></span> Edit</a></li>
                                                    <li>
                                                        <a href="@Url.Action("DeleteVacancyProfile", "Admin", new { id = @item.VacancyProfileID})"><span class="fa fa-exchange danger" style="color:red"></span> Withdraw</a>
                                                    </li>
                                                </ul>
                                            </div>*@
                                        </td>
                                        <td>
                                           <a href="@Url.Action("DeleteVacancyProfile", "Admin", new { id = @item.VacancyProfileID})"><span class="fa fa-exchange danger" style="color:red"></span> Withdraw</a>
                                        </td>
                                    </tr>


                                }
                            }


                        </tbody>
             
                    </table>
                </div>
                <!-- /.box-body -->
            </div>
        </div>

        <!-- /.box-body -->
        <div class="box-footer">
            @*Footer*@
        </div>
        <!-- /.box-footer-->
    </div>
    <!-- /.box -->
</section>

<!-- /.content -->

@if (TempData["message"] != null)
{
    <div class="modal modal-success fade" id="mask" data-toggle="modal">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                    <h4 class="modal-title">Notification</h4>
                </div>
                <div class="modal-body">

                    <p>@Html.Raw(Json.Encode(TempData["message"]).Replace(@"""", ""))</p>

                </div>
                <div class="modal-footer">
                    <button type="button" href="@Url.Action("AllUsers", "Admin")" class="btn btn-default pull-right" data-dismiss="modal">Close</button>
                </div>
            </div>
            <!-- /.modal-content -->
        </div>
        <!-- /.modal-dialog -->
    </div>
}


<!-- /.box-body -->
<div class="box-footer">
    @*Footer*@
</div>
<!-- /.box-footer-->
    </div>
<!-- /.box -->
</section>

<!-- /.content -->

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
<script>
    $(document).ready(function () {
        $('#mask').fadeIn(500);
        $('#mask').fadeTo("slow", 0.9);
        $('.window .close').click(function (e) {
            e.preventDefault();
            $('#mask').hide();
            $('.window').hide();
        });
        $('#mask').click(function () {
            $(this).hide();
            $('.window').hide();
        });

    });
</script>